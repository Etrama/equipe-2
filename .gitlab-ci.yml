stages:
  - deploy

deploy:
  stage: deploy
  image: mcr.microsoft.com/azure-cli
  script:
    - echo "$AZURE_PUBLISH_PROFILE" > publishprofile.xml
    - az webapp deployment source config-zip --resource-group "$AZURE_RESOURCE_GROUP" --name "$AZURE_WEBAPP_NAME" --src-path .
